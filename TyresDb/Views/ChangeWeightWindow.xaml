<Window x:Class="TyresDb.Views.ChangeWeightWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TyresDb.Views"
        mc:Ignorable="d"
        Title="Изменение веса" Height="130" Width="200" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Text="{Binding Weight, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Center" Width="67" Margin="5" FontSize="16">
            <TextBox.InputBindings>
                <KeyBinding  Key="Enter" Command="{Binding EnterKeyDownCommand}"
                             CommandParameter="{Binding Weight, RelativeSource={RelativeSource AncestorType={x:Type TextBox}}}"/>
            </TextBox.InputBindings>
        </TextBox>
        <Button Content=" Сохранить " Command="{Binding ChangeWeightCommand}" Grid.Row="1"  VerticalAlignment="Top" HorizontalAlignment="Center" Margin="5"/>
    </Grid>
</Window>
